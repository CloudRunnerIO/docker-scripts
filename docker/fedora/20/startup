#! /bin/bash

NO_COLORS=1 cloudrunner-node configure --id "$HOSTNAME" --server "$SERVER_ID" --org "$ORG_ID" -o -t DOCKER DEBIAN7

NO_COLORS=1 cloudrunner-node details

NO_COLORS=1 cloudrunner-node run
