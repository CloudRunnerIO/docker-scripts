#! /bin/bash

NO_COLORS=1 cloudrunner-node configure --id "$HOSTNAME" --server "$SERVER_ID" --org "$ORG_ID" -o -t DOCKER OPENSUSE-13.1

NO_COLORS=1 cloudrunner-node details

NO_COLORS=1 cloudrunner-node run
